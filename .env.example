# Omniroute / AIOps Incident Intelligence - Local Environment Configuration

# Custom data directory (defaults to repository data/ directory, or /app/data in Docker)
# AIOPS_DATA_DIR=/app/data

# Default random seed for deterministic generation and model training (default: 42)
# AIOPS_SEED=42

# API Server Host & Port
HOST=0.0.0.0
PORT=8000
